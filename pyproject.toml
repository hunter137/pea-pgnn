[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pea-pgnn"
dynamic = ["version"]
description = "Prior-anchored, structure-preserving neural prediction for time-dependent engineering responses"
readme = "README.md"
authors = [
    { name = "Deyu Liang" },
    { name = "Jinlong Liu" },
    { name = "Lei Xu" },
]
maintainers = [{ name = "Deyu Liang" }]
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "numpy>=1.23",
    "scikit-learn>=1.2",
    "torch>=2.0",
]
keywords = [
    "knowledge-guided learning",
    "physics-guided neural network",
    "temporal extrapolation",
    "concrete shrinkage",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Scientific/Engineering",
]

[project.urls]
Homepage = "https://github.com/hunter137/pea-pgnn"
Documentation = "https://github.com/hunter137/pea-pgnn/tree/main/docs"
Repository = "https://github.com/hunter137/pea-pgnn"
Issues = "https://github.com/hunter137/pea-pgnn/issues"
Changelog = "https://github.com/hunter137/pea-pgnn/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "build>=1.2",
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "ruff>=0.5",
    "twine>=5.1",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "pea_pgnn._version.__version__" }

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --cov=pea_pgnn --cov-report=term-missing --cov-fail-under=80"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    # Keep annotations importable on the oldest supported Python release.
    "UP007",
    "UP045",
]
